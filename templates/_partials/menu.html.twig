
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarText">

            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                {% for option in menu.findOptionsByLeftPos %}
                    {% if option.isActive %}
                        <li class="nav-item">
                            {% if option.link %}
                                <a class="nav-link" href="{{ option.link }}">
                            {% endif %}
                            {{ option.name|raw }}
                            {% if option.link %}
                                </a>
                            {% endif %}
                        </li>
                    {% endif %}
                {% endfor %}
            </ul>

            <ul class="navbar-nav mx-auto mb-2 mb-lg-0">
                {% for option in menu.findOptionsByCenterPos %}
                    {% if option.isActive %}
                        <li class="nav-item">
                            {% if option.link %}
                                <a class="nav-link" href="{{ option.link }}">
                            {% endif %}
                            {{ option.name|raw }}
                            {% if option.link %}
                                </a>
                            {% endif %}
                        </li>
                    {% endif %}
                {% endfor %}
            </ul>

            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
                {% for option in menu.findOptionsByRightPos %}
                    {% if option.isActive %}
                        <li class="nav-item">
                            {% if option.link %}
                                <a class="nav-link" href="{{ option.link }}">
                            {% endif %}
                            {{ option.name|raw }}
                            {% if option.link %}
                                </a>
                            {% endif %}
                        </li>
                    {% endif %}
                {% endfor %}
            </ul>

        </div>

    </div>
</nav>